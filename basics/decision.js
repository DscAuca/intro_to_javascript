var lecturer = 25;
var remaining=0;
const max= 27
const username="kalisa"
if(remaining==0 && lecturer >= max ){
    console.log(username,` can graduate`)
} else{
    console.log(username,` can not graduate`)
}
